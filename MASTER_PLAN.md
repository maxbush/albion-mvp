# 🗺️ MASTER_PLAN v5 — ALBION MVP Fifth Audit (Round 8, Deep Architectural, Product & Logic Audit)

> Создан: 2026-08-02 (Round 8 — тотальный аудит логики, UI/Backend синхронизации, оптимизации и мертвого кода)
> Базовая линия на старте: **203/203 тестов passing** (коммит `7045bcc`)
> Метод E2E-проверки: интерфейс продукта = Telegram-бот (веб-UI нет, **Playwright неприменим** — прецедент зафиксирован в v3/v4). E2E = pytest-харнесс, эмулирующий Telegram-апдейты (`FakeUpdate`/`FakeBot`) с прогоном полных цепочек через event bus + SQLite scheduler. Каждая задача закрывается только с зелёной проверкой.

---

## 📜 История предыдущих раундов

- **Round 1–2:** хардкоды, demo-gating, idempotency, kill switch, timezone-fixes (95/95).
- **Round 3 (v3 файла):** 15/15 задач, 120/120 тестов, org-timezone канон H4/P4.1.
- **Round 4:** UX-пакет U1–U7 (меню по ролям, честные ожидания, confirm опасных действий).
- **Round 5:** визарды координатора (`/schedule`, `/add_student`, `/add_tutor`), perma-серии + occurrence-ядро, `wizard_state` в SQLite.
- **Round 6:** персональная отмена (по занятиям родителя, 2-шаговый confirm), `/lessons` с постоянными ссылками, i18n-слой (RU/EN), авто-сводка 07:30.
- **Round 7 (v4 файла):** тотальный аудит логики, UI/Backend синка, мёртвого кода — закрыто 19 задач (203/203 тестов).
- **Round 8 (этот план): глубокая проверка архитектурной логики, прав доступа ролей, логирования и очистка мёртвого UI/кода.**

---

## 🔴 P0 — Логирование, права доступа ролей и безопасность

- [x] **R8-8. SafeStreamHandler в `src/utils/logging.py`: pre-encode сообщения в `emit`**
  ✅ 2026-08-02. Реализован класс `SafeStreamHandler(logging.StreamHandler)`, метод `emit` предварительно кодирует/декодирует сообщение под кодировку потока (`errors="replace"`), защищая от `UnicodeEncodeError` и вызова `handleError(record)`. 2 новых теста, 205/205.

- [x] **R8-2. Доступ координаторов к инструментам управления: ошибочная блокировка не-админов**
  ✅ 2026-08-02. Создан хелпер `is_coordinator_or_admin(user_id)` в `src/bot/roles.py`, заменены проверки в командах `/schedule`, `/add_student`, `/add_tutor` (`src/bot/wizard.py`), `/incidents`, `/today`, `/morning_digest`, `/leads` (`src/bot/pilot.py`). 1 новый тест, 206/206.

- [x] **R8-3. Команда «/ok» не проверяет права доступа**
  ✅ 2026-08-02. Добавлена проверка `is_coordinator_or_admin(upd.effective_user.id)` в `cmd_ok` (`src/bot/handlers.py`). Обновлен тест `test_cmd_ok_uses_workflow_resolution_and_cancels_future_actions` в `test_runtime_fixes.py` (правило закрытия задачи 2: прежнее поведение без проверок авторизации признано дефектом). 1 новый тест, 207/207.

---

## 🟡 P1 — Недовоплощенное функциональное ядро (Webhook-события)

- [x] **R8-5. Подписчики на вебхук-события LESSON_STARTED и LESSON_COMPLETED**
  ✅ 2026-08-02. В `LessonOpsWorkflow` (`src/workflows/lesson_ops.py`) зарегистрированы обработчики `handle_lesson_started` и `handle_lesson_completed`. При поступлении `LESSON_STARTED` (status="lv") реактивно закрывается активный `class_live_check` и отменяется таймер в scheduler; при `LESSON_COMPLETED` (status="cp") завершаются оставшиеся проверки урока. 1 новый тест, 208/208.

---

## 🟢 P2 — Мертвый код, UI-клинанс и оптимизация

- [x] **R8-6. Мертвые callback-обработчики в handle_callback и неиспользуемые вспомогательные функции**
  ✅ 2026-08-02. Удалены мертвые ветки обработки `role_coordinator`, `role_parent`, `demo_absent`, `demo_report` в `handle_callback` и недостижимая функция `_show_demo_report` из `src/bot/handlers.py`. 1 новый тест, 209/209.

- [x] **R8-7. Оптимизация EventBus: защита от рекурсивных алертов и таймаутов**
  ✅ 2026-08-02. Усилена защита в `EventBus._publish_alert` от исключений при обработке `SYSTEM_DLQ_ALERT`, а также добавлена безопасная проверка имени обработчиков (`getattr(handler, "__name__", str(handler))`) в `src/events/bus.py`. 1 новый тест, 210/210.

- [x] **R8-9. UX Вариант 3: кнопки закрытия в `/incidents` и удаление команды `/ok` из меню координатора**
  ✅ 2026-08-02. Удалена команда `("ok", "Закрыть ситуацию")` из меню `ROLE_COMMAND_MENUS["coordinator"]` (`src/bot/roles.py`). В выдачу команды `/incidents` (`src/bot/pilot.py`) добавлены inline-кнопки `[✅ Закрыть #ID]` к активным инцидентам. Обработчик `coord_resolve:<ID>` разрешён для координаторов и админов. Обновлён тест `test_u1_menu_applied_on_registration` (удаление команды "ok" из меню). 1 новый тест, 211/211.

- [x] **R8-10. UX Вариант 4b: детализация времени опоздания репетитора (`checkin_late_time`)**
  ✅ 2026-08-02. При выборе репетитором кнопки «⏰ Опоздаю» (`checkin:...:late`) бот предлагает микро-шаг выбора интервала: `[на 5 мин]`, `[на 15 мин]`, `[на 30+ мин]` (`checkin_late_time:<wid>:<nonce>:<mins>`). При выборе отправляется детализированное уведомление координаторам и подтверждение репетитору без отправки лишних сообщений (inline-edit). 1 новый тест, 212/212.

---

## 🛑 ТРЕБУЕТСЯ ВМЕШАТЕЛЬСТВО ЧЕЛОВЕКА / ОТЛОЖЕНО ВЛАДЕЛЬЦЕМ

- **R8-1 (Отложено). Добавление роли «student» (🎓 Ученик) в UI-кнопки.** По решению владельца (2026-08-02) отложено: усложняет MVP.
- **R8-4 (Отложено). Воронка статусов лидов (`update_status`, `/lead_status`).** По решению владельца (2026-08-02) отложено на следующий шаг.
- **H1 (П10). Саморегистрация координатора без аппрува.** Любой TG-пользователь может взять роль coordinator. Аппрув роли сознательно отложен владельцем для удобства тестирования.
- **H2 (D6). Включение `merithub_occurrences` в webhook-lookup** (subClassId → parent classId). Решение владельца: продакшен-сторона, вне скоупа текущих аудитов.
- **H3. EN-версия текстов требует вычитки носителем** (тьюторы англоговорящие). Онбординг/плейсхолдеры остаются RU до подтверждения EN-формулировок.
- **H4. WhatsApp-мост** (ограничения Business API собраны). Припарковано владельцем: продакшен-сторона, вне скоупа.
- **H5. `docker-compose` монтирует `albion.db` файлом.** При пустом/отсутствующем файле Docker может создать каталог вместо файла.

---

## 🔧 Правило закрытия задачи

1. Статус `- [ ]` → `- [~]` → код → E2E-проверка (pytest-харнесс, полный прогон suite + новые тесты на функционал) → коммит атомарным сообщением → `- [x]` с датой.
2. Любое падение существующего теста = мой фикс неправ, а не тест устарел — если старое поведение признано дефектом, правлю тест И помечаю это явно в задаче/коммите.
3. Запрещено: глобальные переименования, изменение схемы БД вне миграционного паттерна, ослабление гейтов ролей.
